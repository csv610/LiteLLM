# MedKit Tests

This folder contains integration-style and cross-module tests for the MedKit package.

## What It Includes

- CLI-level tests
- agent tests
- scenario tests
- shared fixtures and helpers

## Notes

- File names use the `mock_test_*.py` pattern rather than the default `test_*.py` convention.
- See `INDEX.md` for a higher-level directory note.
